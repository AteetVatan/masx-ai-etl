 # =============================================================================
 # MASX AI ETL - Production Requirements
 # =============================================================================
 # Optimized for production deployment with GPU acceleration
 # Install with: pip install -r requirements.prod.txt
 
 # Core FastAPI Framework
 fastapi==0.104.1
 uvicorn==0.34.3
 
 # NLP & ML
 torch==2.7.1
 transformers==4.52.4
 sentence-transformers==4.1.0
 nltk==3.9.1
 scikit-learn==1.7.0
 hdbscan==0.8.40
 numpy==1.26.4
 scipy>=1.11.0  # Required for scikit-learn and scientific computing
 
 # Dimensionality Reduction (Required for clustering)
 umap-learn>=0.5.5
 
 # GPU Acceleration (CUDA 12.1)
 --extra-index-url https://download.pytorch.org/whl/cu121
 --extra-index-url https://pypi.nvidia.com
 cupy-cuda12x==13.1.0
 cuml-cu12==24.4.*
 rmm-cu12==24.4.*
 
# Optional GPU Libraries (for enhanced performance)
# GPU-accelerated similarity search
- faiss-gpu>=1.7.4
# FAISS will be installed via micromamba (conda-forge) in the Dockerfile.
# (Removed: PyPI faiss-gpu has no compatible wheel for Py3.11 + CUDA 12.1.)

 pynvml>=11.5.0    # NVIDIA GPU monitoring
 
 # Database and ORM
 supabase==2.17.0
 asyncpg>=0.30.0
 psycopg2-binary>=2.9.0
 
 # Web Scraping
 requests==2.32.4
 beautifulsoup4==4.13.4
 lxml==5.4.0
 crawl4ai>=0.1.0
 playwright>=1.46.0
 
 # Logging and Configuration
 structlog==23.2.0
 python-dotenv==1.1.0
 PyYAML==6.0.2
 
 # Translation and Language Detection
 deep-translator==1.11.4
 langid>=1.1.6
 lingua-language-detector>=1.1.0
 
 # Vector Database
 chromadb==1.0.13
 
 # Retry and Backoff Logic
 tenacity==9.1.2
 backoff==2.2.1
 
 # Data Validation
 pydantic==2.11.7
 pydantic-settings>=2.0.0
 
 # Serverless Platform
 runpod>=0.6.2
